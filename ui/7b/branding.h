#pragma once

/* Display branding shared by firmware and simulator.
 * Change this value and rebuild to rename the display.
 * Use short ASCII text with the current bundled fonts (up to 16 characters).
 */
#ifndef DISPLAY_BRAND_NAME
#define DISPLAY_BRAND_NAME "HYPER ESC"
#endif
